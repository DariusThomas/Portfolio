export default {
    DM:{
        KEY:process.env.DM_KEY,
        SECRET:process.env.DM_SECRET,
        VIRTUAL_NUMBER:process.env.DM_VIRTUAL_NUMBER,
        PERSONAL_NUMBER:process.env.PERSONAL_NUMBER
    }
}